# Move Light

A local-first moving countdown. Pick a date, check tasks off, and download a simple plan. No account or server storage.

Run planner checks with `node --test tests/move-light.test.mjs`.
